import train_and_test

EPISODE_LENGTHS = [100, 300, 500, 700]
MINIBATCH_SIZES = [20, 50, 100, 500]
DISCOUNTS = [0.85, 0.9, 0.95, 0.99]
EPSILON_DECAYS = [0.99, 0.993, 0.995, 9.997]
TARGET_UPDATES = [5, 10, 20, 30]

for EPISODE_LENGTH, MINIBATCH_SIZE, DISCOUNT, EPSILON_DECAY, TARGET_UPDATE in EPISODE_LENGTHS,MINIBATCH_SIZES,DISCOUNTS,EPSILON_DECAYS,TARGET_UPDATES:
    train_and_test.main(EPISODE_LENGTH, MINIBATCH_SIZE, DISCOUNT, EPSILON_DECAY, TARGET_UPDATE)
    
    